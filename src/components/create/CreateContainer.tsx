import React from 'react';
import {View} from 'react-native';
import CustomText from '../custom-component/CustomText';

const CreateContainer = () => {
  return (
    <View>
      <CustomText>생성 화면</CustomText>
    </View>
  );
};

export default CreateContainer;
