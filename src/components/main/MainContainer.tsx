import React from 'react';
import {View} from 'react-native';
import CustomText from '../custom-component/CustomText';

const MainContainer = () => {
  return (
    <View>
      <CustomText>메인 화면</CustomText>
    </View>
  );
};

export default MainContainer;
